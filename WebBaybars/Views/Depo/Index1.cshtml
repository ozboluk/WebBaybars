@model IEnumerable<WebBaybars.Models.Malinyeri>

@{
    ViewBag.Title = "Index1";
}

<h2>Index1</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.miktar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.barcod)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.yeri)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.miktar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.barcod)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.yeri)
        </td>
        <td>
            @Html.ActionLink("Sil", "Delete", new {  id=item.barcod })
        </td>
    </tr>
}

</table>
